{
  "name": "@paullefizelier/wp-to-strapi-web",
  "version": "0.1.0",
  "description": "Nuxt 4 UI for the WordPress → Strapi migrator",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../../packages/core run build",
    "dev": "nuxt dev",
    "prebuild": "npm --prefix ../../packages/core run build",
    "build": "nuxt build",
    "pregenerate": "npm --prefix ../../packages/core run build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "pretypecheck": "npm --prefix ../../packages/core run build",
    "typecheck": "nuxt typecheck"
  },
  "dependencies": {
    "@iconify-json/lucide": "^1.2.133",
    "@nuxt/ui": "^4.11.1",
    "@paullefizelier/wp-to-strapi-core": "^0.1.0",
    "nuxt": "^4.5.2",
    "tailwindcss": "^4.3.3",
    "vue": "^3.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vue-tsc": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
