/** Nuxt UI v4 reads the theme from app.config: colour aliases, then per-component defaults. */
export default defineAppConfig({
  ui: {
    colors: {
      primary: "indigo",
      neutral: "slate",
    },
  },
});
